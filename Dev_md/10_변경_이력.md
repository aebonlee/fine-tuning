# FineTuning - 변경 이력

## v1.0 초기 개발 (ebc190c)
- React 19 + Vite 6 기반 프로젝트 생성
- Supabase PKCE 인증 (Google/Kakao OAuth, 이메일)
- 12개 카테고리, 54개 레슨 구조 설계
- CSS Custom Properties 테마 시스템 (5색상 × 3모드)
- 한/영 다국어 지원
- 레슨 사이드바 + TOC (목차) 자동 생성
- 커뮤니티 게시판 (CRUD + 댓글)
- 소개 페이지 (About, Guide, Roadmap, Tools)
- 홈페이지 히어로 캐러셀 (5슬라이드, 4종 배경 애니메이션)

## 커스텀 도메인 배포 설정 (2b365b7)
- `public/CNAME`: `fine-tuning.dreamitbiz.com`
- `vite.config.js`: base를 `/fine-tuning/` → `/`로 변경
- `public/404.html`: `pathSegmentsToKeep`를 1 → 0으로 변경
- GitHub Pages + gh-pages 패키지로 배포

## Databricks 파인튜닝 콘텐츠 추가 (56b17fb)
- `src/content/fundamentals/what-is-finetuning.js` 신규 작성
- `src/config/lessons.js`에 레슨 추가
- Databricks 블로그 참조 (Full/PEFT/LoRA/QLoRA 방법론)

## OG 미리보기 이미지 생성 (dd51055)
- `scripts/generate-og-images.mjs`로 sharp 라이브러리 활용
- 11개 OG 이미지 (1200×630px) `public/og/`에 생성
- SEO.jsx에서 react-helmet-async로 OG 메타 태그 관리

## 전체 52개 레슨 콘텐츠 추가 (43cb2a9)
- 12개 카테고리 × 평균 4.5개 레슨 = 54개 중 52개 신규 작성
- 모든 파일 `{ contentKo, contentEn }` 한/영 마크다운 형식
- 코드 예제, 테이블, 명령어 포함한 실용적 학습 콘텐츠

## Dev_md 문서 업데이트 (2aef5e8)
- `02_프로��트_구조.md`: 콘텐츠 디렉토리 54개 파일 반영
- `08_빌드_배포.md`: 커스텀 도메인 설정 문서화
- `09_콘텐츠_가이드.md`: 12 카테고리 54개 레슨 목록 신규 작성

## 레슨 페이지 가로 오버플로우 수정 (d65f3d1, f9263e4)
- **원인**: CSS Grid `1fr` = `minmax(auto, 1fr)`로 긴 코드줄이 그리드 확장
- **수정 내용**:
  - `.ck-layout`: `1fr` → `minmax(0, 1fr)` (그리드 트랙 최소크기 제한)
  - `.ck-main`: `min-width: 0; overflow: hidden` 추가
  - `.markdown-body`: `overflow-x: hidden` 추가
  - `.prompt-code-wrapper`: `max-width: 100%; overflow: hidden` ��가

## OG 이미지 URL 도메인 수정 (c35cae4)
- **원인**: 전체 프로젝트에서 `finetuning.dreamitbiz.com` (하이픈 없음) 사용
- **실제 도메인**: `fine-tuning.dreamitbiz.com` (하이픈 있음)
- **수정 파일 8개**:
  - `src/config/site.js` - SITE_CONFIG.url
  - `index.html` - OG/Twitter 메타 태그
  - `src/pages/Home.jsx` - schema.org JSON-LD
  - `src/pages/lessons/LessonDetail.jsx` - breadcrumb JSON-LD
  - `src/utils/auth.js` - OAuth redirect URL
  - `scripts/generate-og-images.mjs` - 이미지 내 URL 텍스트
  - `public/og/*.png` - 11개 이미지 재생성
  - `Dev_md/01_프로젝트_개요.md` - 문서 URL

## 게시판 UI/UX 디자인 개선
- **XSS 취약점 수정**: `dangerouslySetInnerHTML` 제거 → `white-space: pre-wrap`으로 안전한 텍스트 렌더링
- **카테고리 뱃지 색상 개별화**:
  - `tip` 전용 초록 뱃지 (`board-category-tip`) 추가
  - `showcase` 전용 빨강 뱃지 (`board-category-showcase`) 추가
  - 기존: `tip`, `showcase` 모두 `board-category-resource` 공유 → 구분 불가
- **다크모드 완전 지원**:
  - 카테고리 뱃지 6종 다크모드 색상 추가
  - 검색 input, 댓글 textarea 배경색 다크모드 적용
  - 상세 페이지, 작성 폼, 액션 버튼, 페이지네이션 다크모드 추가
- **검색 디바운스 추가** (400ms): 매 키입력마다 API 호출 방지
- **페이지네이션 개선**: 이전/다음 화살표, 최대 5개 페이지 표시, 말줄임(…) 처리
- **아이콘-숫자 간격 수정**: 조회수/댓글수 아이콘과 숫자 사이 간격 추가
- **인라인 스타일 → CSS 클래스**: 빈 게시글 상태를 `.board-empty` 클래스로 전환
- **수정 파일 5개**:
  - `src/pages/community/Board.jsx` - 디바운스, 페이지네이션, 카테고리 매핑
  - `src/pages/community/BoardDetail.jsx` - XSS 수정, 카테고리 매핑, 아이콘 간격
  - `src/styles/community.css` - 뱃지 추가, 콘텐츠 스타일, 빈 상태, 페이지네이션
  - `src/styles/dark-mode.css` - 게시판 다크모드 전면 보강
  - `src/styles/responsive.css` - 480px 페이지네이션 크기 축소

## 사이트 평가 및 모바일 최적화
- **평가보고서 작성**: `Dev_md/11_사이트_평가보고서.md` 종합 평가 (5.9/10)
- **모바일 터치 타겟 확대**:
  - `.btn-sm`: 30px → 40px (min-height: 40px)
  - `.mobile-color-dot`: 28px → 36px
  - `.mobile-ctrl-btn`: 40px → 44px
  - `.toast-close`: 20px → 36px (padding 확대)
  - `.comment-delete-btn`: 23px → 확대 (padding 6px 10px)
  - `.page-btn`: 36px → 40px
  - `.ck-nav-parent`/`.ck-nav-child`: min-height 44px/40px 추가
- **히어로 파티클 애니메이션 모바일 비활성화**: 768px 이하 `display: none`
- **`prefers-reduced-motion` 지원**: 애니메이션/트랜지션 비활성화
- **Toast 모바일 전체 너비**: 480px에서 좌우 8px 간격
- **1024px 브레이크포인트 보강**: Dashboard grid, Settings layout 태블릿 단계에서 1단 전환
- **480px Settings 반응형 추가**: 카드 패딩, 폰트 크기 축소
- **마크다운 테이블 가로 스크롤**: `.table-wrapper { overflow-x: auto }` 추가
- **다크모드 마크다운**: 인라인 코드, blockquote, th 배경 다크모드 오버라이드 추가
- **`will-change: transform` 제거**: 캐러셀 트랙 불필요한 GPU 레이어 생성 방지
- **수정 파일 9개**:
  - `src/styles/responsive.css` - 1024px 보강, 480px Settings, reduced motion
  - `src/styles/hero.css` - 파티클 비활성화, will-change 제거
  - `src/styles/base.css` - btn-sm 터치 타겟
  - `src/styles/navbar.css` - 모바일 컬러 도트, ctrl 버튼 확대
  - `src/styles/toast.css` - 닫기 버튼 확대, 480px 전체 너비
  - `src/styles/community.css` - 페이지네이션/댓글삭제 터치 타겟
  - `src/styles/intro-page.css` - 사이드바 터치 타겟
  - `src/styles/lessons.css` - 테이블 스크롤 래퍼
  - `src/styles/dark-mode.css` - 마크다운 다크모드
  - `src/styles/animations.css` - prefers-reduced-motion

## 평가 점수 개선 (접근성·보안·i18n·UX 강화)
- **React ErrorBoundary 추가**: `src/components/ErrorBoundary.jsx` 신규 → 런타임 에러 시 전체 앱 크래시 방지
- **Skip-to-content 링크**: 키보드 사용자가 네비게이션 건너뛰고 본문으로 이동
- **ARIA 속성 전면 추가**:
  - Navbar: `role="navigation"`, `aria-label`, `aria-expanded`, `aria-haspopup`, `role="menubar/menuitem"`, `role="dialog"` (모바일 메뉴)
  - 드롭다운: `role="listbox/option"`, `aria-selected` (색상 피커), `role="menu/menuitem"` (유저 메뉴)
  - 모바일 색상 선택: `role="radiogroup"`, `role="radio"`, `aria-checked`
  - 아코디언: `aria-expanded` (모바일 메뉴 그룹)
  - Toast: `role="alert"`, `aria-live="polite"`, `aria-label` (닫기 버튼)
  - 삭제 확인 모달: `role="dialog"`, `aria-modal="true"`, `aria-labelledby`
  - 로딩 상태: `role="status"`, `aria-label="Loading"`
  - 아이콘: `aria-hidden="true"` (장식 아이콘)
- **키보드 네비게이션**: Escape 키로 드롭다운/모바일 메뉴/모달 닫기
- **폼 접근성**: `<label htmlFor>` + `sr-only` 클래스로 스크린 리더 지원 (Login, Register, BoardWrite, BoardDetail)
- **`autoComplete` 속성 추가**: 이메일/비밀번호/이름 필드
- **쿠키 보안 강화**: `SameSite=Lax`, `Secure` 플래그 추가, 중복 유틸리티 `src/utils/cookies.js`로 통합
- **Supabase 환경변수 검증**: placeholder 폴백 제거 → 누락 시 콘솔 에러 명시
- **AuthContext 에러 핸들링**: 모든 `then(() => {})` 제거 → 에러 로깅, `signOut` try/catch 추가, `getSession` 에러 처리
- **i18n 하드코딩 제거**:
  - Register.jsx: 영어 하드코딩 → `t('auth.passwordMismatch')`, `t('auth.passwordTooShort')`, `t('auth.checkEmail')`
  - BoardDetail.jsx: `language === 'ko'` 삼항 → `t('community.backToList')`, `t('community.nextPost')` 등 20+ 키
  - BoardWrite.jsx: 하드코딩 → `t('community.writeTitle')`, `t('community.titleLabel')` 등 10+ 키
  - Footer.jsx: 저작권 연도 `2025` → `new Date().getFullYear()` 동적
  - translations.js: 저작권 연도 동적 변경
- **번역 키 40+ 추가**: `community.*` (backToList, nextPost, prevPost, postNotFound 등), `auth.*` (passwordMismatch, passwordTooShort, checkEmail 등)
- **로그인/회원가입 리다이렉트**: 이미 인증된 사용자가 `/login`, `/register` 접근 시 `/dashboard`로 리다이렉트
- **로딩 상태 개선**: `...` 텍스트 → `loading-spinner` 컴포넌트 (Login, Register, BoardWrite)
- **미저장 경고**: BoardWrite에서 작성 중 페이지 이탈 시 `beforeunload` + `confirm` 경고
- **Post Not Found 개선**: 인라인 스타일 → 전용 빈 상태 UI + 목록 돌아가기 버튼
- **Supabase 쿼리 병렬화**: BoardDetail 이전/다음 글 + 댓글 순차 → `Promise.all()` 병렬
- **라우트 순서 수정**: `/community/board/write`를 `/community/board/:id` 앞으로 이동
- **이미지 lazy loading**: 아바타 `<img>` 태그에 `loading="lazy"` 추가
- **등록 입력 검증**: 이름 필드 `maxLength={50}` + `.trim().slice(0, 50)` 서버 전송 전 정리
- **수정 파일 17개**:
  - `src/components/ErrorBoundary.jsx` (신규)
  - `src/utils/cookies.js` (신규)
  - `src/App.jsx` - ErrorBoundary 래핑
  - `src/utils/supabase.js` - 환경변수 검증
  - `src/utils/translations.js` - 40+ 번역 키 추가, 동적 저작권 연도
  - `src/contexts/AuthContext.jsx` - 에러 핸들링 전면 개선
  - `src/contexts/ThemeContext.jsx` - 쿠키 유틸리티 통합
  - `src/contexts/LanguageContext.jsx` - 쿠키 유틸리티 통합
  - `src/contexts/ToastContext.jsx` - ARIA 라이브 리전
  - `src/components/layout/Navbar.jsx` - ARIA, 키보드, skip-to-content
  - `src/components/layout/Footer.jsx` - 동적 저작권 연도
  - `src/layouts/PublicLayout.jsx` - `main#main-content`, 라우트 순서 수정
  - `src/pages/Login.jsx` - 접근성, 리다이렉트, 로딩 스피너
  - `src/pages/Register.jsx` - i18n, 접근성, 리다이렉트, 입력 검증
  - `src/pages/community/BoardDetail.jsx` - i18n, 접근성, 병렬 쿼리
  - `src/pages/community/BoardWrite.jsx` - i18n, 미저장 경고, 접근성
  - `src/styles/base.css` - skip-to-content, sr-only 스타일
  - `index.html` - dns-prefetch

## PageHeader 그라데이션 배너 구현
- **`PageHeader` 재사용 컴포넌트** 신규 생성 (`src/components/PageHeader.jsx`)
  - Props: `icon`, `title`, `description`, `breadcrumbs`, `children`
  - `var(--accent-gradient)` 배경 + 흰색 텍스트 + 장식 원형 pseudo-element
  - 브레드크럼 네비게이션 (react-router-dom Link 연동)
- **7개 페이지 적용**: Dashboard, Settings, Board, BoardWrite, BoardDetail, LessonCategories, LessonList
- **CSS 정리**: 각 페이지 중복 헤더 스타일 제거, padding-top 조정 (nav-height를 PageHeader에 위임)
- **반응형**: 768px/480px 브레이크포인트에서 패딩/폰트/아이콘 축소
- **다크모드 호환**: `[data-theme="dark"]` 어두운 오버레이 적용
- **5색 테마 호환**: `var(--accent-gradient)` 사용하여 자동 대응
- **수정 파일 14개**: PageHeader.jsx(신규), page-header.css(신규), index.css, 7개 페이지, 4개 CSS
- **상세 문서**: `Dev_md/12_PageHeader_그라데이션_배너.md`

## 주요 트러블슈팅

### Git push 충돌
- **문제**: 원격에 CNAME, README.md가 이미 존재하여 push 거절
- **해결**: `git pull origin main --allow-unrelated-histories`

### WebFetch 실패
- **문제**: Databricks 블로그 페이지가 JS 렌더링이라 HTML 가져오기 실패
- **해결**: WebSearch로 검색 결과에서 콘텐츠 수집 후 직접 작성

### OG 이미지 미리보기 불가
- **문제**: 카카오 디버거에서 OG 이미지 로딩 안 됨
- **원인**: 도메인 URL 오타 (`finetuning` → `fine-tuning`)
- **해결**: 전체 프로젝트 URL 일괄 수정 + OG 이미지 재생성
